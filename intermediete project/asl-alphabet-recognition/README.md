# ASL Alphabet Recognition

Project for recognizing American Sign Language (ASL) alphabet using a CNN / transfer learning approach.

## Files
- `file.ipynb` — training / experimentation notebook
- `app.py` — inference / simple app script (if applicable)

## Notes
Large files (dataset, trained models like `.keras`) are ignored via `.gitignore`.